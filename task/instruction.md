Create a JSON file at /app/output.json.

The file must contain a JSON object with these keys:
- input_rows
- output_rows
- duplicates_removed

You can use any tools available in the environment. Keep the solution simple and deterministic.

You have 45 seconds to solve this task.
